import React from 'react';
import { StyledHeader } from './styles/Header-styled';

const Header = () => {
    return (
        <StyledHeader>
            
        </StyledHeader>
    );
}

export default Header;
